# --- Core Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database ---
sqlalchemy==2.0.35
alembic==1.13.2
asyncpg==0.29.0          # async Postgres driver
psycopg[binary]==3.2.13  # sync driver, used by Alembic migrations (psycopg3: better wheel
                         # availability on new Python/Windows than psycopg2-binary)

# --- Validation / Settings ---
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# --- Auth / Security ---
python-jose[cryptography]==3.3.0   # JWT
cryptography==43.0.1                # Fernet symmetric encryption (2FA secrets, OAuth tokens at rest)
passlib[bcrypt]==1.7.4              # password hashing
bcrypt==4.0.1                       # pin: passlib 1.7.4 is incompatible with bcrypt>=4.1 (__about__ removed)
python-multipart==0.0.9             # form/file uploads (avatars)
pyotp==2.9.0                        # TOTP for 2FA

# --- Git Engine ---
pygit2==1.15.1

# --- Object Storage ---
boto3==1.35.24            # MinIO speaks S3 protocol

# --- Cache / Queue ---
redis==5.0.8
celery==5.4.0

# --- Realtime ---
websockets==13.0

# --- AI Integration ---
anthropic==0.34.2

# --- Email ---
aiosmtplib==3.0.2
jinja2==3.1.4              # email templates

# --- Utilities ---
httpx==0.27.2
python-slugify==8.0.4       # repo/org slug generation

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
faker==28.4.1

# --- Dev tooling ---
ruff==0.6.8
mypy==1.11.2
