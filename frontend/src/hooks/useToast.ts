'use client';

/**
 * useToast re-export for ergonomic imports.
 * Keeps the hook in /hooks/ where other hooks live.
 */
export { useToast } from '@/components/shared/ToastProvider';
export type { ToastOptions, ToastSeverity } from '@/components/shared/ToastProvider';
